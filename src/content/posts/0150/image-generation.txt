模式：内置 image_gen，新建图片。

etcd-lease.png
Use case: infographic-diagram. 为中文技术博客绘制清晰严谨的流程图，横向1536x1024左右，白底，深蓝正文，青色正常路径、橙色超时路径。标题「etcd：超时如何变成权限交接」。从上到下六个编号步骤，每步大字号中文，箭头清晰，左侧工作进程A，中间etcd集群，右侧候选进程B。准确文字：①「A 创建租约 L」；②「原子事务：owner 不存在才写入 A，并绑定 L」；③「A 持续 KeepAlive，刷新租约」；④「续租中断 → leader 检出超时」；⑤「Raft 多数派提交撤销 → 删除 owner」；⑥「B 收到 Watch 通知 → 再用原子事务争抢」。底部两条重点：「超时不证明 A 已死」「通知不等于 B 获得权限；成功抢占后仍需 fencing」。集群框中画3个小server图标标明「多数派」。不画每次心跳进入Raft；步骤③为细青色循环箭头，步骤⑤用醒目粗箭头。不要多余装饰，不要加未指定概念，中文必须可读。

zookeeper-session.png
Use case: infographic-diagram. 中文技术博客流程图，1536x1024，白底深蓝字，青色正常路径，橙色到期路径，大字号中文。标题「ZooKeeper：断开连接，不等于会话过期」。上方框「A 建立会话 S」向下箭头到「创建临时节点 /jobs/7/owner」。再到「请求或 PING 维持会话」。再到菱形「连接中断」。从这里分两条并列路径，左绿色标题「会话仍有效」，框「连接另一台服务器」向下到「继续使用会话 S」向下到「临时节点保留，A 继续持有」。右橙色标题「超过会话超时」，框「leader 检测会话到期」向下到「Zab 提交关闭会话」向下到「删除临时节点」向下到「通知 B → B 再尝试原子创建」。下方全宽强调条「A 可能尚未收到过期通知；旧写入仍要由资源端 fencing 拒绝」。右侧细字注解「通知不等于抢占成功」。不要将断连直接连到删除，不要让客户端决定服务端过期。专业技术图，留白充分，无多余装饰，文字完全准确清晰。

