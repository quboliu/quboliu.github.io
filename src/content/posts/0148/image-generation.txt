内置 ImageGen：linearizable-read-flow.png
Use case: infographic-diagram. 中文DDIA博客精确技术图，横向1536x1024白底，大字，深蓝青色橙色。标题「线性一致读：哪些确认不能省」。上半部左右对照：左标题「etcd v2：QGET」，三步「读请求进入 Raft 日志」→「多数派复制并提交」→「按序执行读取」。右标题「etcd v3：ReadIndex」，四步「leader 记录当前 commitIndex 为 R」→「携带请求标识的心跳取得多数派确认」→「读取节点等待 appliedIndex ≥ R」→「读取本地 KV」。右侧注解「读取节点可以是 follower」「确认领导者资格，不是向多数副本取数据投票」。下半部独立一栏「Neon Pageserver：局部对应」，流程「收到页面版本要求」→「等待所需 WAL 摄取到位」→「重建并返回页面」。用虚线仅连接两处等待步骤，标签「相似的是本地追到所需进度」。底部橙色「Safekeeper 的 WAL 安全性不等于一次 ReadIndex；不能据此推导 SQL 线性一致性」。请准确图文，不省略quorum，不画三种机制完全等价，无代码截图。
最终编辑：删除两部分之间的所有虚线；Neon 等待 WAL 摄取到位，页面随后按需重建，不声明等待持久化。
