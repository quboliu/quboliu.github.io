# 发布前事实审阅记录

文章：计算机系统的数字直觉：从 CPU 周期到数据库吞吐量。
核查日期：2026-09-13 UTC。

本文件以下划线命名，不作为独立博文收录。文章正文的每项来源位于 index.md；本记录保留审阅过程，不替代原始资料。

## Kimi 独立审阅

使用用户指定的 `/home/xuntingmu/.kimi-code/bin/kimi`，要求读取全文、联网核查、复算公式，并区分错误、缺少限定与无法核验。审阅仅对本篇内容进行，不授权其提交或发布。

- 全文审阅会话：`session_56af194f-b5b6-42d2-a144-f9f95b79e841`。Kimi 按来源分组核查后给出的总体结论：未发现阻断发布的事实错误；建议补充若干测试条件及口径说明。
- 补充审阅会话：`session_b53b1e04-b189-426d-a993-108b5a0fc923`。专门核查后补的 Skylake Client 缓存、Kingston DRAM 时序、Redis 本机与网络通信延迟。三部分均通过。

全文首轮所读为较早初稿，因此其“缺少缓存数据”和“本地 intel.pdf 并非 i9 规格书”评论需结合补充审阅理解：最终版本中的 intel.pdf 引用用于 Intel 优化手册的缓存表，i9 规格引用 Intel ARK，二者没有混用。

## 已处理的意见

- Intel 最高频率增加 Thermal Velocity Boost 条件；AMD DDR5 接口增加 1DPC 条件。
- io2 Block Express 延迟增加 Nitro 实例前提，保留平均值、I/O 大小和设计目标措辞。
- Samsung 顺序吞吐实验补全内存、主板及操作系统配置。
- MySQL 沿用来源 TPS 标记并明确 POINT_SELECT 负载；核心数沿用原文 48 CPU cores 表述。
- Kafka 明确 broker 异步刷盘、producer 不等确认、不保证消息实际送达，以及批量实验接近网络带宽上限。
- 位与字节的单位引用改为 NIST；资料日期明确使用 UTC。

## 独立复核与保留边界

- 作者侧直接核对官方页面和原始 PDF，并用计算程序复核周期、带宽、IOPS、容量、带宽时延积、在途请求数、尾概率、可用性和电量换算。
- Azure 四对地域延迟以直接打开的官方正文为准，保留 2026-07-30 数据集日期，未采用过期搜索摘要。
- Google SRE 的 43.2 分钟、4.32 分钟、25.9 秒与原表一致；不采用平均自然月口径替换。
- MySQL 页面确有 Long Lived Connections 小节；原文可直接定位 Figure 6/7 及所引数值。
- 未采纳将 HDD 随机写加入表格、扩展更多产品型号等可选建议，避免引入额外未经完整审阅的比较。
- Redis 配置披露不全的数字仍标“官方示例”；PostgreSQL 9.2 的数字仍标“历史官方发布宣称”。两者不作为生产容量保证。

## 发布校验

已完成正文格式检查、Astro 检查与静态构建，引用定义无缺失；页面生成 21 张表格、30 个来源引用定义。审阅后修改均为上述限定与术语修订，并再次通过构建。
