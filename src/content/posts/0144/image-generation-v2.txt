Mode: built-in ImageGen
Prompt:
生成一张中文技术博客信息图，横版约1600x1100，白色背景、深蓝标题、清晰无衬线中文大字、淡蓝和浅绿分区。不要人物或花纹。主题标题准确写“能读到什么，取决于原子操作留下什么”。副标题“b 代表完整提议；各方案都允许读取共享状态”。
主体是四行三列对照表，列标题“接口与原子操作”“共享状态”“其他节点读取的结果”。
第1行左侧蓝色标签“CAS + 普通读取”，操作代码“CAS(D, 空值, b)”；中间绿色框“D = b”；右侧“read(D) → b”，小字“只允许首次从空值写入，之后不改”。
第2行左侧“共享日志”，操作“追加提议 b”；中间画日志框“位置 0：b”，下面较浅色“位置 1：a”；右侧“read(0) → b”，小字“第一条确定后，后来者不能插到前面”。
第3行左侧“取值并加一 + 普通读取”，操作“fetchAdd(counter, 1)”；中间“counter = 3”；右侧“read(counter) → 3”，小字“读得到计数，读不到 0 号对应的提议”。这一行不用绿色而用浅橙色。不要显示共享的号码映射。
第4行左侧“增强的中心服务”，操作“原子地发号并保存提议”；中间“entry[0] = b”，并用小字注明“号码与内容一起确定”；右侧“read(0) → b”，小字“比裸计数器更强的接口”。
表格每行从操作指向共享状态再指向读取结果，箭头细而明确。
底部两行强调文字：“CAS 没有额外的胜者表：写进去的 b 就是决定值。”“增加查询不够；原子地保存号码与提议的绑定，才补上能力。” 最下方浅色小字“以上是接口保证。跨机器容错仍需由底层实现兑现。” 中文逐字准确，不出现投票、Raft、Paxos，不把CAS指令本身画成包含读取接口，必须保留CAS + 普通读取标签。
