Mode: built-in ImageGen
Prompt:
生成一张用于中文技术博客的清晰信息图，横版约 1600×1200，白底、深蓝标题、蓝绿配色，失败处少量橙红色。中文无衬线大字，布局严谨、箭头不交叉，不要装饰插画。主题为“胜者崩溃后，其他节点还能读到什么？”。
画面上部为三个等宽对照栏，每栏有标题、一个操作、一份共享状态、结论。三栏都用“B 的提议为 b”。
第一栏标题“CAS”；操作“把空值改为 b”；共享状态框“决定值 = b”；B 旁有崩溃标记；从共享状态向 A、C 画读取箭头；结论“仍能读出 b”。
第二栏标题“共享日志”；操作“追加完整提议 b”；状态框内画位置和内容：“0 : b”“1 : a”“2 : c”；B 旁有崩溃标记；A、C 从位置 0 读取；结论“仍能读出第一条 b”。
第三栏标题“取值并加一”；操作“B 取得 0 后崩溃”；共享状态“计数器 = 3”；列小字“A 取得 1”“C 取得 2”；从计数器向问号画箭头；结论“谁拿到 0？计数器没有记录”。必须避免画共享的“0→B”映射，否则错误。B拿到0只是画在B本地，使用虚线小框标“B 的本地结果”，与计数器分开。
中间横条准确写“关键不是只选出一个，而是留下所有节点都能读取的决定。”
画面下部是持续订座流程图：标题“从一次决定到持续处理”。从左到右四个大框：“收到请求”→“确定下一条命令”→“按序更新座位状态”→“按请求 ID 保存结果”。第二框下写“CAS：每槽一个只写一次的决定”以及“共享日志：直接连续追加”；第三框下写“订座、取消都进入同一序列”；第四框下写“重试返回原结果”。最下面细字“共识决定命令顺序；业务规则决定订座成功或失败。”。文字保持上述准确含义，整体易读，留足间距，不出现 Raft/Paxos，不画多数派投票，不暗示三个客户端是存储副本。
