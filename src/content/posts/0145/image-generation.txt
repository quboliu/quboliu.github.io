内置 ImageGen：serial-transactions.png
Use case: infographic-diagram. 中文技术博客流程图，白底深蓝字，青色箭头，1536x1024横向，大号中文。标题「共享日志如何得到可串行化事务」。从左至右：两个小框「甲提交 T甲」「乙提交 T乙」汇入「共享日志确定顺序」「第1条 T甲 / 第2条 T乙」，再分流至上下两个相同副本框。每个副本内按上下顺序画「执行完整 T甲：读取→判断→写入→结束」，然后「执行完整 T乙：读取→判断→写入→结束」。两副本初始状态标注「S12 空闲」。两副本结果都为「甲订座成功；乙发现已占用」。底部两条原则「按序执行完整事务 → 实际串行执行 → 满足可串行化」「相同初始状态 + 确定性事务 → 相同进度时结果一致」。强调事务内部含读取与判断，不能只排列最终写入。不把两个事务并行画在一个副本里。专业清晰排版，文字可读，无代码块。
定向编辑：两处 T乙 的写入步骤改为不修改。
