内置 ImageGen：discovery-cache.png
Use case: infographic-diagram. 中文技术博客专业信息图，横向1536x1024白底，大字号中文，深蓝字、青色信息传播、橙色强调。标题「服务发现：后台维护一致，前台允许缓存」。上半部标注「注册信息更新」，流程由左「实例上线 / 下线」箭头到中「权威注册表」「通过共识提交变更」，注册表用3台服务器小图标。中间从权威注册表向下箭头到「注册表副本 / 客户端缓存」，箭头标签「传播、刷新」。下半部明确画客户端从缓存读取地址，再向「业务服务 P1、P2」发请求，业务请求不要穿过权威注册表。文字「查询已有地址」「直接访问服务」。底部重点框「注册中心暂时不可达，已有地址仍可能可用」「缓存允许过时；访问失败需要重试或刷新」。准确表达这是一种可选实现，不是所有服务发现都必须用共识；标题下小字「一种使用共识，但不要求每次查询都取得最新状态的实现」。大幅留白，清晰箭头，无ASCII无代码截图。
定向编辑：删除缓存与业务服务之间直接连线，仅客户端访问业务服务。
